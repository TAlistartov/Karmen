@model Karmen.Models.ComponentModel

<div class="form-inline">
    <label for="component_Id">Выбор из БД:</label>
    @Html.DropDownListFor(x => x.Id, Model.AllComponentsFromDb, "-Комплектующие-", new { @class = "dropdown-toggle form-control mb-2 mr-sm-2 selectFromDb" })
</div>
