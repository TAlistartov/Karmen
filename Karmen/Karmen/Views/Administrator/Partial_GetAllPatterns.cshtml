@model Karmen.Models.PatternModel

<div class="form-inline">
    <label for="pattern_Id">Выбор из БД:</label>
    @Html.DropDownListFor(x => x.Id, Model.AllPatternsFromDb, "-Выберите лекало-", new { @class = "dropdown-toggle form-control mb-2 mr-sm-2 selectFromDb" })
</div>
