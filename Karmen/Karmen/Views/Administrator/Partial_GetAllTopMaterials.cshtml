@model Karmen.Models.TopMaterialModel

<div class="form-inline">
    <label for="topMaterial_Id">Выбор из БД:</label>
    @Html.DropDownListFor(x => x.Id, Model.AllTopMaterialsFromDb, "-Материал верха-", new { @class = "dropdown-toggle form-control mb-2 mr-sm-2 selectFromDb" })
</div>
