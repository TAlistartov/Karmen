@model Karmen.Models.LiningModel

<div class="form-inline">
    <label for="lining_Ddl">Выбор из БД:</label>
    @Html.DropDownListFor(x => x.SelectedLining, Model.AllLiningsFromDb, "-Выбор подкладки-", new { @class = "dropdown-toggle form-control mb-2 mr-sm-2 selectFromDb" })
</div>
