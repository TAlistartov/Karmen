@model Karmen.Models.FootBedModel

<div class="form-inline">
    <label for="footbed_Id">Выбор из БД:</label>
    @Html.DropDownListFor(x => x.Id, Model.AllFootBedsFromDb, "-Выберите стельку-", new { @class = "dropdown-toggle form-control mb-2 mr-sm-2 selectFromDb" })
</div>
